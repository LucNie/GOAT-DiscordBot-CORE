const cc = require('../../core/console')
async function execute(client, dataController) {
    cc.info('neko.init','🐈 neko powering up...')
}

module.exports = {
    name: 'init',
    description: '',
    execute
}

